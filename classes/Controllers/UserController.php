<?php

require_once dirname(__FILE__) . '/Controller.php';

class UserController extends Controller
{





    
    // создать строку в таблице
    // public function create($data)
    // {
        
        
    //     if (array_key_exists('username', $data) && array_key_exists('first_name', $data) && array_key_exists('last_name', $data)) {
    //         $username = $data['username'];
    //         $firstName = $data['first_name'];
    //         $lastName = $data['last_name'];

    //         return $this->entity->create($username, $firstName, $lastName)->execute();
    //     }
    //     return ['code' => 500, 'message' => 'id or name not found'];

    // }
}
